#include "main.h"

/**
 * _islower - Shows if input is in lowercase
 * @c: Character that checks case
 *
 * Return: 1 for lowercase and 0 for other.
 */

int_islower(int c)
{
	if (c >= 97 && c <= 122);
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
